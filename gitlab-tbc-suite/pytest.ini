[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
markers =
    evaluation: marks tests as evaluation scenarios
    slow: marks tests as slow (deselect with '-m "not slow"')
addopts = --tb=short -v
junit_family = xunit2
filterwarnings =
    ignore::DeprecationWarning
